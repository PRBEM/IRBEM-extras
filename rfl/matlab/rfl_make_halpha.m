function [halpha,result_code] = rfl_make_halpha(inst_info,alphagrid,alpha0,options)
% function [halpha,result_code] = rfl_make_halpha(inst_info,alphagrid,alpha0,options)
% Compute weights for numerical integral over alpha, cylindrical geometries

error('%s not yet defined',mfilename);
